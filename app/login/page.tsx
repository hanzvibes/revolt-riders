"use client";
import {getSupabaseBrowserClient} from "@/lib/supabase/client";
import {ArrowLeft,LockKeyhole,Mail,ShieldCheck,UserPlus} from "lucide-react";
import {useEffect,useState,type FormEvent} from "react";
export default function LoginPage(){const[mode,setMode]=useState<"login"|"register">("login"),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[memberId,setMemberId]=useState(""),[message,setMessage]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);useEffect(()=>{void getSupabaseBrowserClient().auth.getUser().then(({data})=>{if(data.user)setMessage(`Sudah masuk sebagai ${data.user.email??"member"}.`)})},[]);const submit=async(e:FormEvent)=>{e.preventDefault();setLoading(true);setError("");setMessage("");try{const supabase=getSupabaseBrowserClient();if(mode==="login"){const{error}=await supabase.auth.signInWithPassword({email,password});if(error)throw error;window.location.assign("/profil")}else{const{error}=await supabase.auth.signUp({email,password,options:{emailRedirectTo:`${window.location.origin}/login`,data:{member_external_id:memberId.trim().toUpperCase()}}});if(error)throw error;setMessage("Pendaftaran diterima. Cek email untuk verifikasi, lalu tunggu validasi pengurus.")}}catch(cause){setError(cause instanceof Error?cause.message:"Autentikasi gagal.")}finally{setLoading(false)}};return <main className="auth-page"><a href="/" className="back-link"><ArrowLeft/>Kembali</a><section className="auth-card"><img src="/revolt-riders-logo.jpg" alt="Revolt Riders"/><em>MEMBER ACCESS</em><h1>{mode==="login"?"Masuk ke Revolt":"Daftar akun member"}</h1><p>{mode==="login"?"Gunakan akun yang sudah diverifikasi pengurus.":"Member ID akan dicocokkan dengan data resmi club."}</p><form onSubmit={submit}>{mode==="register"&&<label>Member ID<div><UserPlus/><input value={memberId} onChange={e=>setMemberId(e.target.value)} placeholder="RR-014" required/></div></label>}<label>Email<div><Mail/><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="nama@email.com" required/></div></label><label>Password<div><LockKeyhole/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} minLength={8} required/></div></label>{error&&<p className="error-message">{error}</p>}{message&&<p className="success-message"><ShieldCheck/>{message}</p>}<button className="primary-action" disabled={loading}>{loading?"Memproses…":mode==="login"?"MASUK":"DAFTAR AKUN"}</button></form><button className="mode-switch" onClick={()=>{setMode(mode==="login"?"register":"login");setError("");setMessage("")}}>{mode==="login"?"Belum punya akun? Daftar":"Sudah punya akun? Masuk"}</button></section></main>}
