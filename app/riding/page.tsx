"use client";
import {AppShell} from "@/components/app-shell";
import {useMemberAccess} from "@/hooks/use-member-access";
import {getSupabaseBrowserClient} from "@/lib/supabase/client";
import {Bike,CheckCircle2,Gauge} from "lucide-react";
import {useState,type FormEvent} from "react";
export default function RidingPage(){const{account,loading}=useMemberAccess();const[start,setStart]=useState(""),[end,setEnd]=useState(""),[message,setMessage]=useState(""),[error,setError]=useState(""),[saving,setSaving]=useState(false);const activeAccount=account?.status==="active"?account:null;const distance=Math.max(0,Number(end||0)-Number(start||0));const submit=async(e:FormEvent)=>{e.preventDefault();if(!activeAccount)return;setSaving(true);setError("");const supabase=getSupabaseBrowserClient();const{data:{user}}=await supabase.auth.getUser();if(!user){setError("Sesi login tidak ditemukan.");setSaving(false);return}const{error}=await supabase.from("ride_logs").insert({member_external_id:activeAccount.member_external_id,odometer_start:Number(start),odometer_end:Number(end),submitted_by:user.id,status:"pending"});if(error)setError(error.message);else{setMessage(`${distance.toLocaleString("id-ID")} KM dikirim dan menunggu validasi Road Captain.`);setStart("");setEnd("")}setSaving(false)};return <AppShell active="Riding" title="Catat Riding"><div className="page-wrap"><section className="form-card card"><div className="form-heading"><Bike/><span><em>RIDE LOG</em><h2>Input odometer</h2><p>Jarak dihitung otomatis dan baru masuk statistik setelah disetujui.</p></span></div>{loading?<p>Memeriksa akun…</p>:!activeAccount?<div className="notice">Akun member harus aktif untuk mencatat riding. <a href={account?"/profil":"/login"}>{account?"Lihat status akun":"Masuk sekarang"}</a></div>:<form onSubmit={submit}><label>Odometer awal<input type="number" min="0" step="0.1" value={start} onChange={e=>setStart(e.target.value)} required/></label><label>Odometer akhir<input type="number" min={start||"0"} step="0.1" value={end} onChange={e=>setEnd(e.target.value)} required/></label><div className="distance-preview"><Gauge/><span><small>JARAK TERHITUNG</small><b>{distance.toLocaleString("id-ID")} KM</b></span></div>{error&&<p className="error-message">{error}</p>}{message&&<p className="success-message"><CheckCircle2/>{message}</p>}<button className="primary-action" disabled={saving||distance<=0}>{saving?"Mengirim…":"KIRIM RIDE LOG"}</button></form>}</section></div></AppShell>}
