/* eslint-disable react-hooks/set-state-in-effect */
"use client";
import {AppShell} from "@/components/app-shell";
import type {EventRecord} from "@/lib/domain";
import {formatEventDate,formatShortDate} from "@/lib/domain";
import {getSupabaseBrowserClient} from "@/lib/supabase/client";
import {CalendarDays,Clock3,MapPin,RefreshCw} from "lucide-react";
import {useCallback,useEffect,useState} from "react";
export default function AgendaPage(){const[events,setEvents]=useState<EventRecord[]>([]),[view,setView]=useState<"upcoming"|"history">("upcoming"),[loading,setLoading]=useState(true),[error,setError]=useState("");const load=useCallback(async()=>{try{const{data,error}=await getSupabaseBrowserClient().from("events").select("id,title,slug,type,description,location_name,location_url,start_at,end_at,meetup_at,status").in("status",["published","completed"]).order("start_at",{ascending:true});if(error)throw error;setEvents((data??[]) as EventRecord[])}catch(cause){setError(cause instanceof Error?cause.message:"Agenda gagal dimuat.")}finally{setLoading(false)}},[]);useEffect(()=>{void load()},[load]);const now=Date.now();const upcoming=events.filter(event=>event.status==="published"&&new Date(event.start_at).getTime()>=now);const history=events.filter(event=>event.status==="completed"||new Date(event.start_at).getTime()<now).sort((a,b)=>new Date(b.start_at).getTime()-new Date(a.start_at).getTime());const visible=view==="upcoming"?upcoming:history;const refresh=()=>{setLoading(true);setError("");void load()};return <AppShell active="Agenda" title="Agenda"><div className="page-wrap"><section className="page-intro"><div><em>AGENDA KOMUNITAS</em><h2>Jadwal Revolt Riders</h2><p>Agenda resmi, lokasi kumpul, dan waktu keberangkatan dalam WIB.</p></div><button onClick={refresh} disabled={loading}><RefreshCw className={loading?"spin":""}/>Refresh</button></section><div className="agenda-tabs" role="tablist" aria-label="Jenis agenda"><button role="tab" aria-selected={view==="upcoming"} className={view==="upcoming"?"active":""} onClick={()=>setView("upcoming")}><CalendarDays/>Mendatang <b>{upcoming.length}</b></button><button role="tab" aria-selected={view==="history"} className={view==="history"?"active":""} onClick={()=>setView("history")}><Clock3/>Riwayat <b>{history.length}</b></button></div>{error&&<p className="error-message">{error}</p>}{loading&&<p className="system-message">Memuat agenda…</p>}<div className="event-grid">{visible.map(event=>{const d=formatShortDate(event.start_at);return <article className="agenda-detail" id={event.slug} key={event.id}><time><b>{d.day}</b><small>{d.month}</small></time><div><div className="agenda-labels"><em>{event.type}</em>{view==="history"&&<small>{event.status==="completed"?"SELESAI":"TERLEWAT"}</small>}</div><h3>{event.title}</h3><p>{event.description??"Informasi lengkap akan diperbarui oleh pengurus."}</p><span><CalendarDays/>{formatEventDate(event.start_at)}</span><span><MapPin/>{event.location_name??"Lokasi menyusul"}</span>{event.location_url&&<a href={event.location_url} target="_blank" rel="noreferrer">Buka lokasi</a>}</div></article>})}</div>{!loading&&!error&&visible.length===0&&<section className="empty-state card">{view==="upcoming"?<CalendarDays/>:<Clock3/>}<h2>{view==="upcoming"?"Belum ada agenda mendatang":"Belum ada riwayat agenda"}</h2><p>{view==="upcoming"?"Agenda yang dipublikasikan pengurus akan muncul otomatis di sini.":"Agenda yang selesai akan tersimpan sebagai histori komunitas."}</p></section>}</div></AppShell>}
