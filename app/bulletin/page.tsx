"use client";import {AppShell} from "@/components/app-shell";import {getSupabaseBrowserClient} from "@/lib/supabase/client";import {Bell} from "lucide-react";import {useEffect,useState} from "react";type Bulletin={id:string;title:string;body:string;published_at:string|null};export default function Page(){const[items,setItems]=useState<Bulletin[]>([]),[loading,setLoading]=useState(true);useEffect(()=>{void getSupabaseBrowserClient().from("announcements").select("id,title,body,published_at").eq("is_published",true).order("published_at",{ascending:false}).then(({data})=>{setItems((data??[]) as Bulletin[]);setLoading(false)})},[]);return <AppShell active="Bulletin" title="Bulletin"><div className="page-wrap"><div className="bulletin-list">{items.map(item=><article className="card" key={item.id}><Bell/><div><em>PENGUMUMAN</em><h2>{item.title}</h2><p>{item.body}</p></div></article>)}</div>{!loading&&items.length===0&&<section className="empty-state card"><Bell/><h2>Belum ada pengumuman</h2><p>Bulletin resmi pengurus akan tampil di sini.</p></section>}</div></AppShell>}
