export type EventRecord = { id:string; title:string; slug:string; type:"kopdar"|"riding"|"touring"|"social"|"other"; description:string|null; location_name:string|null; location_url:string|null; start_at:string; end_at:string|null; meetup_at:string|null; status:"draft"|"published"|"cancelled"|"completed" };
export type AnnouncementRecord = { id:string; title:string; body:string; published_at:string|null };
export type PublicInvitation = { event_id:string; event_title:string; event_slug:string; event_type:string; event_description:string|null; location_name:string|null; location_url:string|null; start_at:string; end_at:string|null; meetup_at:string|null; member_external_id:string; rsvp_status:"attending"|"declined"|"maybe"|null; guest_count:number; responded_at:string|null };
export const formatEventDate=(value:string)=>new Intl.DateTimeFormat("id-ID",{timeZone:"Asia/Jakarta",weekday:"long",day:"numeric",month:"long",year:"numeric",hour:"2-digit",minute:"2-digit",hour12:false}).format(new Date(value))+" WIB";
export const formatShortDate=(value:string)=>{const date=new Date(value);return{day:new Intl.DateTimeFormat("id-ID",{timeZone:"Asia/Jakarta",day:"2-digit"}).format(date),month:new Intl.DateTimeFormat("id-ID",{timeZone:"Asia/Jakarta",month:"short"}).format(date).toUpperCase()}};
